# The local web demo needs exactly one secret.
# Get a key from https://fal.ai/dashboard/keys and never commit the filled .env.
# The account behind the key needs a positive balance: https://fal.ai/dashboard/billing
FAL_KEY=
